export default function TickRounded({ className = "w-5 h-5" }) {
  return (
    // eslint-disable-next-line @next/next/no-img-element
    <img src="/images/tick-rounded.svg" alt="" className={`${className} flex-shrink-0`} />
  );
}
